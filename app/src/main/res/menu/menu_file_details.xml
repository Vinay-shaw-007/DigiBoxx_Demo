<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/menu_file_detail">

<!--    <item
        android:id="@+id/cardFullScreenFile"
        android:icon="@drawable/ic_full_screen"
        android:title="@string/fullscreen"
        app:showAsAction="ifRoom"
        android:visible="false"
        tools:visible="true"/>-->
    <item
        android:id="@+id/cardDownloadFile"
        android:icon="@drawable/ic_icon_download"
        android:title="@string/download"
        app:showAsAction="ifRoom" />
    <item
        android:id="@+id/cardMenuFile"
        android:icon="@drawable/ic_three_dot_menu"
        android:title="@string/kabab_menu"
        app:showAsAction="always" />
</menu>